const start=document.getElementById('start'), camera=document.getElementById('camera'), video=document.getElementById('video'), statusEl=document.getElementById('status');
document.getElementById('startBtn')?.addEventListener('click', async()=>{try{const stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:'environment'}},audio:false});video.srcObject=stream;start.hidden=true;camera.hidden=false;statusEl.textContent='CAMERA ACTIVE';}catch(e){statusEl.textContent='CAMERA DENIED';alert('Camera access was denied. Please allow camera permission and try again.');}});
